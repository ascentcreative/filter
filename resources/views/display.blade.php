<{{ $tag }} class="filter-display" style="flex-basis: 100%"
    data-filtersetup="{{ Crypt::encryptString(json_encode($attributes->getAttributes())) }}"
    >
    

    {{-- Ideally, the page would load with the first page of results in place. Is this doable? --}}
    

</{{ $tag }}>

